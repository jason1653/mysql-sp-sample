CREATE DEFINER=``@`%` FUNCTION `fn_RAND_STRING`(
    in_RANGE INT
	,in_NUMBER INT
) RETURNS varchar(100) CHARSET utf8mb4 COLLATE utf8mb4_unicode_ci
BEGIN
    RETURN LPAD(FLOOR(RAND()*in_RANGE), in_NUMBER, "0");
END